#include <iostream>
#include"GameController.h"
#include "Windows.h"
#include "TextureManager.h"
int main()
{

	//GameController().run();
	Windows window(10, 10);
	TextureManager m_texture;

	return 0;
}
